<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Line separator between labels and content in MovieDetailsActivity -->
    <style name="LineSeparator">
        <item name="android:layout_marginEnd">24dp</item>
        <item name="android:layout_marginStart">24dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/colorLineSeparator</item>
    </style>

    <!-- Labels above major content (Overview, Videos and Reviews) in MovieDetailsActivity -->
    <style name="MajorContentLabels">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">24dp</item>
        <item name="android:layout_marginTop">28dp</item>
        <item name="android:textSize">18sp</item>
    </style>

    <!-- Major content (Overview, Videos and Reviews) spacing in MovieDetailsActivity -->
    <style name="MajorContentSpacing">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginEnd">24dp</item>
        <item name="android:layout_marginTop">8dp</item>
    </style>

    <!-- Minor content (Release date and Vote average) in MovieDetailsActivity -->
    <style name="MinorContent">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:textColor">@color/colorTextContent</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
